<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <Target Name="BuildTizenPackage" AfterTargets="Build">
    <Exec Command="tizen package -t tpk -o $(MSBuildProjectDirectory)" />
  </Target>
</Project>
